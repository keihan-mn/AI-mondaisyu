# 自習問題ビューアー要件

## 目的

隣接する `社内研修_AI特別版/preview/` のデザインと操作感を参考に、自習問題を読みながら問題ごとに回答できるローカルビューアーを作る。回答はMarkdownとして保存・ダウンロードでき、保存済みの回答も同じビューアーで閲覧できるようにする。

## 利用形態

- 各受講者が自分のローカルPCへPrivate GitHubリポジトリをクローンして利用する。
- 新しい問題はGitで配布し、受講者は `git pull` で追加分を取り込む。
- ログイン、共有サーバー、複数人の同時利用は対象外とする。
- 問題とビューアーはGit管理し、個人回答はGit管理しない。

## 問題の読み込み

- `02-自主勉強課題/**/*.md` を問題ファイルとして読み込む。
- Markdownの `##` を章、`### 問題N` を回答対象として解析する。
- 現在のGit課題は4章・12問として認識できること。
- 既存の問題Markdownと `.claude/skills/` は保持する。

## 回答

- 各問題の直下に回答欄を表示する。
- 回答欄ではMarkdownを入力でき、入力表示とプレビューを切り替えられること。
- 回答途中の内容をブラウザの `localStorage` へ自動保存する。
- 問題本文が更新されても、既存の下書きを保持する。
- 部分回答を許可し、教材単位で回答をまとめて保存する。
- 保存のたびに日時付きの別ファイルを作り、既存回答を上書きしない。
- 「回答を保存」と「Markdownをダウンロード」は別の操作にする。
- 出力するMarkdownには、作成日時、元問題のパス、回答数、各問題の本文・ヒント・回答を含める。
- 未回答の問題も出力し、未回答であることを明記する。

## 回答ファイル

- 個人回答は `answers/` 配下へ保存する。
- `answers/` は `.gitignore` に登録する。
- 左サイドバーで「問題」と「回答」を切り替え、保存済み回答を一覧から閲覧できるようにする。
- 回答Markdownは既存ビューアーと同じ読みやすいスタイルで表示する。

## UI

- 元ビューアーの紺色サイドバー、朱色のアクセント、本文書体、検索、目次、集中表示、レスポンシブ対応を引き継ぐ。
- 画面上の表示名は日本語のままとする。
- リポジトリ名とローカルフォルダー名は `AI-mondaisyu` とする。
- 講義編集、スライド区切り、Cloudflare、データベース機能は持ち込まない。
- リポジトリ直下で `npm install`、`npm run dev` を実行できる構成にする。

## 保存APIの安全要件

- 問題一覧と保存済み回答一覧を取得できること。
- 保存可能な入力元を、実際に存在する問題ファイルへ限定する。
- 保存先パスやファイル名をクライアントから自由に指定させない。
- パストラバーサル、別オリジン、不正JSON、過大な保存要求を拒否する。
- ファイル名は教材名と保存日時からサーバー側で生成する。
- 排他的な新規作成を行い、回答ファイルを上書きしない。
- 問題・回答Markdownの変更を起動中の画面へ通知する。

## GitとGitHub

- ローカルでは `main` ブランチでGit管理する。
- 将来作成するGitHubリポジトリ名は `AI-mondaisyu` とし、Privateにする。
- GitHubの所有者は未指定のため、所有者が決まるまでリモート作成と `origin` 登録を行わない。
- 現在のGitHub CLI認証は無効であり、接続時に再認証が必要。

## 完了条件

- `npm run build` と自動テストが成功する。
- デスクトップとスマートフォン幅で主要操作を確認する。
- 問題閲覧、回答入力、自動下書き保存、プレビュー、回答保存、ダウンロード、回答閲覧が一連で動作する。
- `git pull` 相当の教材更新後も `answers/` とブラウザ下書きが残る。

