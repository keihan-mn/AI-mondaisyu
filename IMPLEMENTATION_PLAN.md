# 自習問題ビューアー実装計画

## 1. アプリ構成

- リポジトリ直下へVite＋Reactのローカルアプリを構築する。
- 紺色と朱色を基調とするデザイン、Markdown表示、検索、目次、集中表示、レスポンシブ設計を実装する。
- 講義編集、スライド区切り、Cloudflare、データベースは実装せず、このリポジトリに必要な依存パッケージだけを使用する。
- ローカルAPIはViteの開発サーバープラグインとして実装し、問題と回答の読み書きを担当させる。

## 2. 問題データ

- `02-自主勉強課題/` 配下だけを再帰的に走査する。
- 問題文書を、教材、章、問題の構造へ解析する純粋関数を作る。
- 章番号と問題番号から、下書き保存に使用する安定した問題IDを生成する。
- 問題ファイルのパスと内容から更新判定用の署名を生成する。
- 自習問題作成スキルへ、`##` の章と `### 問題N` の互換形式を検証する指示を追加する。

## 3. 回答入力画面

- 左サイドバーに「問題」「回答」の切替を置く。
- 問題一覧は教材フォルダー単位でまとめ、全文検索できるようにする。
- 本文上部に教材説明と共通ルールを表示し、その後に章と問題カードを並べる。
- 各問題カードにMarkdownの回答欄、入力・プレビュー切替、回答状態を表示する。
- 回答済み件数を教材全体の進捗として表示する。
- 入力は短い間隔で `localStorage` へ自動保存する。
- 保存済み下書きと最新問題の署名が異なる場合は、回答を消さずに更新通知を表示する。

## 4. Markdown出力

- 回答Markdown生成を、ブラウザとサーバーの両方から利用できる純粋関数にする。
- 出力は、教材タイトル、元問題パス、作成日時、回答数、章、問題文、ヒント、回答の順に構成する。
- 空欄の回答は未回答として明示する。
- ダウンロードは現在の画面上の回答から生成する。
- サーバー保存時は、サーバー側で最新の問題を読み直してMarkdownを生成する。

## 5. ローカル保存API

- 一覧取得APIは、問題文書と `answers/` 配下の回答文書を返す。
- 回答保存APIは問題パスと回答だけを受け取り、保存先をサーバー側で決定する。
- `answers/<教材名>/<教材名>-answer-YYYYMMDD-HHmmss.md` を基本形式とする。
- 同じ名前が存在する場合は連番を追加し、排他的な新規作成で保存する。
- JSONサイズ、Content-Type、同一オリジン、問題パス、回答値の型を検証する。
- 問題・回答Markdownを監視し、Server-Sent Eventsで変更を通知する。

## 6. 保存済み回答の閲覧

- 回答モードでは、保存日時と元教材が分かる一覧を表示する。
- 選択した回答Markdownを読み取り専用で表示する。
- 回答内の見出しから右側の目次を作る。
- 問題追加や回答保存を、画面再起動なしで一覧へ反映する。

## 7. 配布とドキュメント

- `.gitignore` に `answers/`、`node_modules/`、ビルド成果物、開発キャッシュ、`.DS_Store` を登録する。
- READMEに、初回クローン、依存関係の導入、起動、回答保存、ダウンロード、`git pull` による更新方法を書く。
- ローカルでGitを初期化し、`main` ブランチを使用する。
- GitHub接続は所有者の指定と再認証後の残作業とする。

## 8. テスト

- 現在のGit問題から13章・39問・39個のヒントを取得できること。
- 回答Markdownが問題文を含み、部分回答と未回答を正しく表現すること。
- 同じ教材を連続保存しても別ファイルになり、上書きしないこと。
- 保存後の回答が一覧取得へ反映されること。
- 不正パス、存在しない問題、別オリジン、不正JSON、過大要求を拒否すること。
- 下書きの保存・復元と、問題更新時の保持を確認すること。
- `npm run build`、自動テスト、デスクトップ・スマートフォンでのブラウザ確認を行うこと。
