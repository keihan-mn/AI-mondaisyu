# 引き継ぎ

## 現在の状況

- Vite＋Reactによる自習問題ビューアーをリポジトリ直下へ実装済み。
- `02-自主勉強課題/**/*.md` の解析、問題ごとのMarkdown回答、入力／プレビュー切替、進捗表示、検索、目次、集中表示、レスポンシブ表示を実装済み。
- `localStorage` への下書き自動保存と、問題更新時に回答を保持したまま通知する仕組みを実装済み。
- 日時付き回答Markdownのダウンロードと、`answers/` への排他的な新規保存、保存済み回答の一覧・閲覧を実装済み。
- 問題・回答Markdownの変更通知、入力元制限、サーバー側ファイル名生成、パストラバーサル・別オリジン・不正JSON・過大要求の拒否を実装済み。
- 自習問題作成スキルへビューアー互換形式の検証項目を追加済み。
- README、`.gitignore`、自動テストを整備済み。
- `npm test` は8件すべて成功、`npm run build` も成功。
- 実ブラウザでデスクトップ幅、スマートフォン幅、回答入力、プレビュー、下書き復元、回答保存、回答閲覧を確認済み。ブラウザ警告・エラーは0件。
- ブラウザ確認で作成したサンプル回答が `answers/01-Git/` に1件ある。`answers/` はGit管理対象外。
- ローカルGitは `main` ブランチで初期化済み。まだコミットはなく、リモートも未登録。

## 最初に読む資料

1. `REQUIREMENTS.md` — 確定した目的と要件
2. `IMPLEMENTATION_PLAN.md` — 実装方針とテスト計画
3. `02-自主勉強課題/01-Git/README.md` — 現在の問題形式
4. `.claude/skills/creating-self-study-questions/SKILL.md` — 今後の問題作成フロー
5. 隣接する `社内研修_AI特別版/preview/` — デザインと操作感の参照元

## 次の作業

1. GitHubの所有者を決める。
2. GitHub CLIを再認証する。
3. GitHub上に `AI-mondaisyu` というPrivateリポジトリを作成する。
4. ローカルリポジトリへ `origin` を登録し、`main` をpushする。

## 注意点

- 隣接する `社内研修_AI特別版` は参照専用。変更しない。
- 既存の問題Markdownとスキルを、明示的な指示なしに削除・上書きしない。
- 個人回答は `answers/` へ保存し、Git管理しない。
- GitHubの所有者は未指定。Privateリポジトリ作成と `origin` 登録は行わない。
- GitHub CLIの既存認証は無効。接続前に再認証が必要。
- ブラウザ確認用の回答はサンプルであり、必要かどうかを利用者が判断する。

## 完了時の更新

実装完了後、このファイルの「現在の状況」「次の作業」「注意点」を実態に合わせて更新する。新しい引き継ぎファイルは作らない。
